# Task hiện tại: (chưa có)

## Các bước

- [ ] 1. ...

## Đã chốt

-

## Lưu ý cho agent tiếp theo

- Import CSV clone jobs: sheet của user dùng header `topicName` / `url`, còn importer cần `topicName (Brand)` / `book url` (+ `Select`, `Niche`, `Priority`). Phải chuyển header trước khi import; luôn chạy `?dryRun=1` trước.
